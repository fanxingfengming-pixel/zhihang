# 安全与隐私边界

职航会处理简历、岗位描述、投递记录和 Offer 信息。这些内容可能包含个人信息，默认只应用于当前求职实训流程。

## 已实现的控制

- DeepSeek、Qwen 与 Supabase Secret Key 仅保存在服务端内存或服务端环境变量，不进入浏览器存储、Prompt、Career Profile 或 Supabase 快照。
- 设置页只接受 DeepSeek 与阿里云 DashScope 官方 HTTPS 地址，避免把密钥转发到任意服务。
- 简历、JD、用户输入和上下文均被标记为不可信数据；嵌入其中的指令不能覆盖 Agent 系统规则。
- 模型输出先经过 JSON 解析、Zod Schema、Agent 事实守卫和凭据脱敏，再返回页面。
- Agent、设置、简历上传和云同步接口限制请求大小与频率，并拒绝跨站写请求。
- 所有包含求职资料的 API JSON 响应使用 `private, no-store`，页面使用防嵌入与基础 CSP 安全响应头。
- PDF / DOCX 简历在同源服务端内存中即时生成，响应禁止缓存；导出不会调用大模型、写入数据库或上传到第三方。
- Supabase 工作区使用显式权限与 RLS，只允许登录用户访问自己的记录。
- 实时岗位缓存表启用并强制执行 RLS，撤销匿名与普通登录角色的直连权限，只允许服务端 Secret Key 访问；公开 API 只返回规范化后的岗位字段。
- 招聘来源仅使用公开 JSON API，所有 HTML 描述都会转为纯文本，外链必须是无账号信息的 HTTPS 地址。
- 生产环境的共享模型密钥使用模型白名单，并通过 Supabase 原子预留实施账号分钟/每日请求、每日/月度 Token、哈希网络身份和并发任务的分布式配额。
- 共享模型用量写入私有表，只记录请求编号、Agent、模型、状态、耗时、Token 与可选成本；不记录简历、JD、回答、API 密钥正文或原始 IP。
- 自由对话的 Career Profile 与 JD 上下文默认关闭，用户逐项授权后才会发送，并可预览字段；档案上下文排除姓名与完整简历正文。
- 提供公开存活检查和受 Bearer Token 保护的深度健康检查；预算达到 80% 时可向管理员 HTTPS Webhook 发送不含用户正文的告警。
- 账号注册、登录和密码重置可接入 Cloudflare Turnstile，并把挑战令牌交给 Supabase Auth 验证；Secret Key 不进入本项目环境变量。

## 使用者须知

- 不要在简历或 JD 中粘贴密码、API 密钥、身份证完整号码、银行卡号等与求职无关的敏感数据。
- DeepSeek/Qwen 真实模式会将当前任务所需资料发送给所选模型提供商；若不希望发送，请使用演示模式。
- 浏览器本地模式的数据由当前浏览器保管。共用电脑使用后应清理浏览器站点数据。
- AI 结果可能有误，简历、Offer 条款和关键求职决策应由本人核实。

## 当前限制

一般非模型接口仍使用进程内防突发限制；会产生平台费用的共享模型路径已经使用 Supabase 分布式额度。自带用户密钥模式默认只在本地开发开放，生产环境不得依赖进程内设置。正式公开前仍必须在账号控制台配置日志保留、自定义 SMTP、CAPTCHA、外部健康探测与备份恢复，并定期检查 Supabase Security Advisor。仓库无法替代这些账号侧设置。
